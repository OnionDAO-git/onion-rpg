# Reinstalled inside the image
node_modules

# Build artifacts (regenerated by `bun run build`)
build
.svelte-kit
.output

# Hardware / non-server trees not needed to build the game server
beacon
oRPG
firmware-ext
sim
tests
docs

# Local + tooling
.git
.gitignore
.env
.env.*
!.env.example
.DS_Store
.claude
.codex
.agents
.orpg-workflow.js

# Deploy plumbing (not needed inside the image)
deploy
docker-compose.yml
railway.json
README.md
SPEC.md
